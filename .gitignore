# SPDX-FileCopyrightText: Copyright (c) 2025-2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0

.vscode/
__pycache__/
.DS_Store
dev/
results/
archived/
*.egg-info/
.VSCodeCounter/
build/
dist/
*/automation/dynamo/
aiconf_save/
.collector_checkpoint/
venv/
env/
.venv/

*.pyc
*.pyo
*.pyd

# Rust build artefacts and workspace lockfile.
target/
Cargo.lock

# Local scan working artefacts (parity scan SQLite, logs, probe traces).
*.sqlite
*.sqlite-shm
*.sqlite-wal
.scan-probe.log
scan-pareto*.log
scan-pareto.log.*
planner_reports/

# maturin-built Rust extension (compiled locally / shipped in wheel)
aic-core/src/aiconfigurator_core/*.so
aic-core/src/aiconfigurator_core/*.pyd

tools/gemm_dvfs_char_unused

# GEMM / comm / overlap characterization: run logs, traces and caches are
# regenerable by the scripts in those directories and should not be committed.
tools/*/logs_*.txt
tools/*/*_run.log
tools/*/sweep.log
tools/*/*.out
tools/overlap_exp/trace_*.json
tools/overlap_exp/*_out.txt
# 223 files / 9.1 MB of figures and CSVs from sweeps that later runs superseded.
# Kept on disk for reference; not worth carrying in the repo.
tools/comm_dvfs_char/_superseded/
